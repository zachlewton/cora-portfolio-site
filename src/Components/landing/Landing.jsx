import React, { useState, useEffect } from 'react';
import { NavLink } from 'react-router-dom';

const Landing = (props) => {
	return (
		<div>
			<NavLink to="/home">coralina</NavLink>
		</div>
	);
};

export default Landing;
